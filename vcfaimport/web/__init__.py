"""Browser console for vcfa-import: `vcfa-import serve`.

server.py speaks HTTP, api.py maps requests onto the engine and state store,
jobs.py runs the long operations in the background, static/ is the UI.
"""
